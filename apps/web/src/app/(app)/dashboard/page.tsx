import { MyWorkDashboard } from '@/features/dashboard/pages/MyWorkDashboard';

export const metadata = {
  title: 'Tổng quan công việc — Tasks',
};

export default function DashboardPage() {
  return <MyWorkDashboard />;
}
