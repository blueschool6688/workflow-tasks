import { ProjectListPage } from '@/features/projects/pages/ProjectListPage';

export const metadata = {
  title: 'Danh sách Dự án — Tasks',
};

export default function ProjectsPage() {
  return <ProjectListPage />;
}
